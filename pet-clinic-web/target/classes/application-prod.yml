spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_petclinic_prod
    username: sfg_petclinic_prod_user
    password: haowei
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false